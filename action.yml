#                █████
#               ░░███
#       ██████  ███████    ██████
#      ███░░███░░░███░    ░░░░░███
#     ░███ ░███  ░███      ███████
#     ░███ ░███  ░███ ███ ███░░███
#     ░░██████   ░░█████ ░░████████
#      ░░░░░░     ░░░░░   ░░░░░░░░
#
#   Copyright (C) 2026 — 2026, Ota. All Rights Reserved.
#
#   DO NOT ALTER OR REMOVE COPYRIGHT NOTICES OR THIS FILE HEADER.
#
#   Licensed under the Apache License, Version 2.0. See LICENSE for the full license text.
#   You may not use this file except in compliance with that License.
#   Unless required by applicable law or agreed to in writing, software distributed under the
#   License is distributed on an "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND,
#   either express or implied. See the License for the specific language governing permissions
#   and limitations under the License.
#
#   If you need additional information or have any questions, please email: os@ota.run

name: ota
description: Run ota in GitHub Actions and publish summaries, annotations, comments, and receipt artifacts.
author: ota.run

inputs:
  command:
    description: "Ota command to run. Supported values: doctor, receipt."
    required: false
    default: receipt
  path:
    description: Repo or contract target passed to Ota.
    required: false
    default: .
  baseline:
    description: Optional receipt baseline passed to `ota receipt --baseline`; supports `latest` or a receipt JSON file path.
    required: false
  baseline-artifact-name:
    description: Restore the latest successful artifact with this name from the current workflow on the default branch and use its archived receipt as the compare baseline.
    required: false
  fail-on-new-blockers:
    description: Add `--fail-on-new-blockers` for receipt baseline compares and fail only on newly introduced blocker findings.
    required: false
    default: "false"
  working-directory:
    description: Working directory used when invoking Ota.
    required: false
    default: .
  execution-mode:
    description: "Ota execution mode. Supported values: native, container."
    required: false
    default: native
  member:
    description: Optional monorepo member target.
    required: false
  archive:
    description: Add --archive when command=receipt.
    required: false
    default: "true"
  annotate:
    description: Emit GitHub annotations from Ota findings.
    required: false
    default: "true"
  max-annotations:
    description: Maximum findings to emit as annotations.
    required: false
    default: "20"
  comment-pr:
    description: Create or update a sticky pull request comment.
    required: false
    default: "false"
  comment-pr-only:
    description: Only comment when the workflow event is a pull request.
    required: false
    default: "true"
  artifact-name:
    description: Uploaded artifact name.
    required: false
    default: ota-report
  artifact-retention-days:
    description: Optional artifact retention in days.
    required: false
  fail-on-error:
    description: Fail the action when Ota reports a blocked outcome.
    required: false
    default: "true"
  install:
    description: "Ota installation mode. Supported values: auto, always, never."
    required: false
    default: auto
  ota-version:
    description: Optional Ota version to install when the action performs installation.
    required: false
  ota-bin:
    description: Ota binary name or path to use after installation resolution.
    required: false
    default: ota
  output-path:
    description: Where the captured Ota JSON output is written.
    required: false
    default: .ota-action-output.json
  github-token:
    description: Token used to create or update pull request comments.
    required: false

outputs:
  ok:
    description: Whether Ota reported an ok result.
  status:
    description: "Derived action status: ready, risky, or blocked; receipt diff gates can report risky when baseline debt remains but no new blockers were introduced."
  output-path:
    description: Path to the captured Ota JSON output.
  archive-path:
    description: Archived receipt path when receipt --archive produced one.
  baseline-path:
    description: Resolved receipt baseline path when a compare baseline was used.
  artifact-name:
    description: Uploaded artifact name.
  error-count:
    description: Error count from the Ota summary.
  warn-count:
    description: Warning count from the Ota summary.
  info-count:
    description: Info count from the Ota summary.
  gate-rule:
    description: Diff gate rule when a receipt compare gate was active.
  gate-passed:
    description: Whether the diff gate passed when a receipt compare gate was active.
  primary-summary:
    description: Primary blocker or top finding summary.

runs:
  using: node24
  main: dist/index.js
